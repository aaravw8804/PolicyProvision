spring.application.name=PolicyProvision
server.port=8080

# No database is used anywhere in this application - all data is held
# in-memory via java.util.concurrent collections and is lost on restart.

logging.level.com.telusko.policyprovision=INFO
